# Media server accounts

Real Jellyfin and Emby truth for account creation, library scope, switching access off and on, and linking (by an admin, or by the user proving an existing account is theirs with its password). Request/response contracts, RBAC, rollback ordering, and log redaction are proven by the hermetic suites; every case here reads the resulting state on the media server itself (`GET /Users/{id}`, a sign-in as the created user, and `GET /Users/{id}/Views` for library scope, all three of which both servers answer). The cases that say Jellyfin run against Jellyfin and again against Emby; `MSRV-014` and `MSRV-015` are Emby-only, for the two places its account flow differs.

Use the [run template](../run-template.md) to record executions of these cases.

## Account creation, scope, and linking

- [ ] `MSRV-001` · P0 · UI/LIVE — Add a Jellyfin instance with a real API key; verify Test Connection passes, the Shared libraries section lists exactly the server's libraries under its reported name, and the chosen library ids plus the sign-in address survive a save and reopen.
- [ ] `MSRV-002` · P0 · UI/LIVE — As a granted user, create an account from the guide with a chosen password; verify `GET /Users` on Jellyfin shows the user with `IsAdministrator=false`, a sign-in with that password succeeds, and the Cantinarr database holds no trace of the password.
- [ ] `MSRV-003` · P0 · LIVE — Create an account whose username already exists on the server; verify the app reports the name as taken, no Jellyfin user is created or altered, and linking that existing account from Users records the row without changing the Jellyfin user.
- [ ] `MSRV-009` · P1 · UI/LIVE — Create with two of three libraries chosen; verify `GET /Users/{id}/Views` as the new user lists only those two, and a fourth library added on Jellyfin afterwards stays hidden.
- [ ] `MSRV-010` · P1 · UI/LIVE — Create with no libraries chosen, then add a library on Jellyfin; verify the user sees it with no Cantinarr action.
- [ ] `MSRV-013` · P1 · UI/LIVE — With accounts already created, untick a shared library in the instance editor and save; verify `GET /Users/{id}/Views` for an existing Cantinarr-created account no longer lists it, that an account the admin linked keeps the libraries it had, and that re-ticking restores it.
- [ ] `MSRV-014` · P0 · LIVE — Emby: create an account from the guide; verify `GET /Users/{id}` reads `HasPassword=true` and `IsAdministrator=false`, a sign-in with the chosen password succeeds and one with an empty password is refused. Then make the password step fail (block `POST /Users/{id}/Password` through a proxy) and, separately, make the create answer unreadable (a proxy that forwards `Accept-Encoding` but drops `Content-Encoding` does it); verify no account survives on Emby either way.
- [ ] `MSRV-015` · P0 · LIVE — Emby: create with two of three libraries chosen; verify the policy's `EnabledFolders` holds the folder `Guid`s that `GET /Library/MediaFolders` reports (never the numeric `Id`), `GET /Users/{id}/Views` lists only those two, and unticking one in the editor removes it from the existing account.
- [ ] `MSRV-016` · P0 · UI/LIVE — As a granted user with a pre-existing account on the server, link it from the guide with **I already have an account** and the real password; verify the row is recorded as linked (not created by Cantinarr), the account's policy on the server is unchanged, the Cantinarr database and server logs hold no trace of the password, the sign-in check's session is logged out again (`GET /Sessions` as an admin lists none for the "Cantinarr sign-in check" device) and at most one such device exists on the dashboard however many checks ran, and the guide shows the account with where to sign in.
- [ ] `MSRV-017` · P0 · UI/LIVE — Link with a wrong password, then with the right one; verify the app says the password was wrong and stays signed in to Cantinarr (a refused password is never a revoked session), the server's own failed-attempt counter moved exactly as its login page would move it, and nothing was linked until the right password.
- [ ] `MSRV-018` · P0 · UI/LIVE — Link the server's administrator account, from the guide with its password and again from the admin picker (where it is listed and marked); verify the guide shows the "Cantinarr never changes it" line, removing the grant and deleting the Cantinarr user both leave the account enabled on the server, the maintenance sweep stays quiet afterwards (no retry logged every five minutes), and a shared-library change in the editor leaves its policy alone.

## Audiobookshelf

- [ ] `MSRV-023` · P0 · UI/LIVE — On a disposable Audiobookshelf server with two libraries, create an administrator API key and connect it in Cantinarr. Create an account with one library selected, prove its password works and only that library appears, link an existing local account with its password, and import a third account from Users. Verify remote permissions and temporary-session cleanup directly in Audiobookshelf. For the Cantinarr-created account, revoke and restore its grant and change shared libraries; verify the resulting remote access while listening history survives. Link the root/admin account and verify grant removal, user deletion, and library changes leave it untouched.
- [ ] `MSRV-024` · P1 · UI/LIVE — With an available Chaptarr audiobook scanned into Audiobookshelf, check an exact ISBN/ASIN match, two distinct copies with different narrators/libraries, a title-only match, and a copy hidden by the linked user's library, tag, or explicit-content restrictions. Open verified links while signed in as that user on web, iOS, and Android; confirm the correct browser item page, including a reverse-proxy path prefix. Stop Audiobookshelf, then scan newly arrived audio or change a restriction directly there and resume/refresh Cantinarr: uncertain results use the general Open shortcut and exact links converge to current access. Record real device and Audiobookshelf versions separately from the disposable Go smoke test.

## Watching

- [ ] `MSRV-019` · P1 · UI/LIVE — As a user with a linked Jellyfin account restricted to some libraries, open an available title that is in a shared library and one that is only in a library not shared; verify the first shows **Watch on Jellyfin** whose web link opens that exact item's page on the server (signed in as that user), the second shows the disabled "Not on Jellyfin yet", a title Radarr has that Jellyfin has not scanned yet reads the same way, and the server's request log shows the lookup made as the user's own account id.
- [ ] `MSRV-020` · P1 · UI/LIVE — Emby: the same with a linked Emby account; verify the button in web opens the item page (`web/index.html#!/item?id=…&serverId=…`) and that stopping the Emby container while the page is open leaves neither a button nor a "not yet" line for it (no answer is never shown as absence).
- [ ] `MSRV-022` · P1 · UI/LIVE — On iOS and Android, tap an available title's **Watch on Jellyfin/Emby** button and an active account's guide **Open** button with each official app installed. Verify Jellyfin opens its home screen, Emby follows a confirmed title link (the guide opens its home), and neither opens the browser or starts playback automatically. Repeat with the receiving app closed, already running, and signed out, and with multiple servers configured. Record app versions and title navigation separately from app handoff. With each app absent, verify the original exact title URL or guide public address opens once in the browser, including reverse-proxy path prefixes. Record Plex device coverage under `PLEX-097`.

## Importing

- [ ] `MSRV-021` · P1 · UI/LIVE — From Users, import two accounts that exist only on the server (one of them switched off there) plus the server's administrator (ticked by hand, since Select all skips it); verify a Cantinarr user of each exact name appears granted and linked (`created_by_cantinarr=0`), each new user's connect link redeems, the accounts' policies on the server are untouched, including the switched-off account when management is unchecked, an account already linked to someone cannot be picked, an account whose name matches an existing Cantinarr user is attached to that user without a new link, and importing the same accounts again answers "already linked" for each and creates nothing. Repeat on Emby.

## Access off and on

- [ ] `MSRV-004` · P0 · UI/LIVE — Remove a managed user's grant in the instance editor; verify the Jellyfin user reads `IsDisabled=true`, the server leaves that user's guide entirely (the admin's Users list shows No grant, the management mode, and Off on server), and granting again flips it back with watch history and library scope intact. Then disable the account in Jellyfin's own UI while the grant stays: the guide keeps listing the server and shows the access-is-turned-off line.
- [ ] `MSRV-005` · P0 · LIVE — Delete a Cantinarr user with a managed account; verify the Jellyfin user is disabled, not deleted, and the account row is gone.
- [ ] `MSRV-006` · P1 · UI/LIVE — Set, change, and clear the sign-in address; verify the guide shows and copies the exact address; Open uses the installed app home on iOS/Android or that address when absent (and on web), and clearing it shows the ask-your-admin line.

## Blindness, exposure, and secrets

- [ ] `MSRV-007` · P1 · CHAOS/UI — Stop the Jellyfin container and open the guide; verify an existing account renders from Cantinarr's record with the could-not-confirm line rather than as no account, and that a create attempt fails with the generic message and leaves no half-created user once Jellyfin returns.
- [ ] `MSRV-008` · P0 · SEC — As an ungranted requester, call `POST /api/media-servers/{id}/account` with a real and a made-up instance id; verify both answer the identical 403 body, `GET /api/media-servers` and `/api/config` carry no instance URL, and every admin media-server route answers 403 for the requester role.
- [ ] `MSRV-011` · P1 · SEC — Capture server logs and the app's HTTP debug log while creating an account; verify neither contains the API key or the chosen password and that `/api/media-servers/…` paths are cut after the scope segment.
- [ ] `MSRV-012` · P0 · CHAOS/LIVE — Stop the Jellyfin container, remove a managed user's grant (the save succeeds), then start Jellyfin again and wait for the maintenance sweep; verify the account reads `IsDisabled=true` without any further admin action and sign-in is refused. Repeat in the other direction: revoke with the server up, stop it, re-grant, restart, and verify the account is re-enabled by the sweep.

## Account management modes

- [ ] `MSRV-025` · P0 · UI/LIVE — On real Jellyfin, Emby, and Audiobookshelf, link/import an existing disabled account with management unchecked. Verify its remote state and libraries stay unchanged after link, grant removal/regrant, and Cantinarr user deletion. Repeat with a self-linked active account. Enable management explicitly on a disposable existing account: verify it follows the current grant and revoke/regrant without changing its library policy.
- [ ] `MSRV-026` · P0 · CHAOS/UI/LIVE — Queue a managed access change while the media server is offline. Verify Users shows the saved grant and pending change separately. Stop managing while offline, restart Cantinarr and the provider, and verify no deferred disable/enable or shared-library update happens. Unlink separately and verify both the remote account and Cantinarr grant remain intact.

- [ ] `MSRV-027` · P0 · UI/LIVE — On real Audiobookshelf, Jellyfin, Emby, and Plex, use a disposable managed account or accepted share with an exact matching title in an allowed library and a matching title in a restricted library. Revoke its Cantinarr grant, stop management while remote access is off, and regrant: access must stay off and no exact link may appear. Restore access directly on the media server (accept the Plex share if needed), refresh Cantinarr, and verify the allowed title's exact external link returns while the restricted title stays unavailable and the account remains Linked only. For Plex, narrow the accepted share while its server still exposes the previous libraries and verify no exact link for a newly restricted title appears, including before the recipient view converges. Disable/remove access directly again and verify the exact link disappears. Check both Cantinarr's response and the receiving account's actual library access; Jellyfin/Emby admin-key item reads alone do not prove a disabled account can sign in. Repeat unlink/relink and explicit management adoption, confirming library policies are preserved, no read changes access, and grant removal or unlinking during a slow lookup cannot return its old link.

## Audiobookshelf libraries per user

- [ ] `MSRV-028` · P0 · UI/LIVE — On one disposable ABS server with two scanned libraries, use Cantinarr’s instance editor to assign one library to Julian and the other to Yana before they create accounts. Create both accounts through the guide. Sign in freshly as each recipient and verify their catalog contains only their assigned library, direct access to the other library is refused, and exact Listen links show only permitted copies. Change the server default and confirm individual selections remain unchanged. Remove and restore a grant, restart Cantinarr, and verify the same selection persists. With ABS stopped, save a different selection, confirm pending status, restart ABS and wait for maintenance to apply it; repeat with stopping management/unlinking before recovery and verify the canceled change does not apply.
