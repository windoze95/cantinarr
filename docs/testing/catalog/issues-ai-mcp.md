# Issues, AI, and MCP

Remediation journeys with a real AI provider, real provider account/OAuth truth, and real-client MCP ceremonies. Issue lifecycle contracts, tool authorization, scrubbing, and the per-tool MCP contracts are proven by the hermetic suites.

Use the [run template](../run-template.md) to record executions of these cases.

## Issue remediation with a real provider

- [ ] `ISS-016` · P0 · LIVE — Have the agent ask a reporter-only question; verify Awaiting your reply, requester-safe prompt, bounded reply, same-run resume, and no duplicate run.
- [ ] `ISS-024` · P0 · LIVE — Approve every supported action type; confirmation must repeat exact target, one executor call occurs, durable result appears, and issue/run state advances truthfully.
- [ ] `ISS-043` · P0 · LIVE — On a real Sonarr season holding files the service imported before those episodes aired, report wrong content and approve the agent's delete-and-blocklist fix; verify on the service itself that exactly the named episode files are gone from disk, that each grab that delivered one is marked failed and now appears in that instance's blocklist, and that the single approval also replaces what it removed: with the instance's failed-download policy on, the service does the searching and Cantinarr adds none of its own; with it off, Cantinarr searches exactly the aired episodes still missing a file and leaves the unaired ones for the service to grab as they air. Repeat with the policy set both ways, and confirm no second approval is ever requested.
- [ ] `ISS-044` · P0 · LIVE — Without reporting anything, let a real Sonarr with instant updates configured import several files into a season whose episodes have not aired yet. Verify Cantinarr opens exactly one auto-detected issue for the whole season -- never one per file -- labelled `Content that has not aired yet`, that its admin push arrives after the usual hold-down rather than per import, that the agent investigates unprompted and proposes the same single delete-and-blocklist fix, and that approving it closes the issue on its own without an admin verdict. Remove that instance's Connect webhook, repeat the import, and confirm the identical single issue appears from the fallback pass within about fifteen minutes.
- [ ] `ISS-045` · P1 · LIVE — Read every entry of the prevention catalog against a current Sonarr and Radarr and confirm the advice is still true of the build in front of you: open each screen a notice names (Settings > Media Management > Importing minimum free space; Settings > Download Clients > Remote Path Mappings, category, and completed-download folder; Settings > Indexers minimum seeders, minimum age, and priority; Settings > Profiles > Delay Profiles; the quality profile's minimum size) and verify it exists at that path and does what the notice says it does. Menu paths and setting names move between service versions and no hermetic suite can know today's, so this is the one part of the feature the Go tests cannot prove -- and advice that sends an admin to a screen that no longer exists is worse than no advice at all. Confirm also that the notice only ever tells the admin what to change, never offers to change it, and that it names the instance by its display name rather than its URL.

## AI provider accounts, OAuth, and chat

- [ ] `AI-007` · P0 · LIVE — For each personal/shared provider, save a valid provider+model+key candidate; verify one real tool-free low-reasoning turn completes before atomic activation.
- [ ] `AI-013` · P0 · LIVE/UI — Complete personal OAuth for each OAuth provider (OpenAI Codex, xAI Grok); verify account identity (and Codex usage windows), model validation, selected source, restart persistence, and chat.
- [ ] `AI-014` · P0 · LIVE/UI — Repeat the device flow for each admin-shared OAuth provider; verify only admins see shared identity/plan/usage and granted users learn only that access is included.
- [ ] `AI-018` · P0 · UI/LIVE — Start chat and verify ordered SSE frames: conversation ID, text, tool start/end, media results, error if any, then `[DONE]`; app never leaves a permanent typing state.
- [ ] `AI-019` · P0 · API/UI/LIVE — Against disposable current Radarr, Sonarr, and Chaptarr instances, explicitly ask in-app chat to make one scalar/cutoff/custom-format-score profile change; verify the assistant shows the preview and autonomously applies it once in the same authenticated turn without asking the admin to copy a reference. Confirm a different device, later turn, invented reference, replay, or direct arr edit cannot use the one-use handoff. In Settings > Configuration history, verify the applied entry identifies the target and actor, projects before/applied/current differences without raw snapshots, and offers restore only while the live profile, dependencies, and instance binding match. Restore the original values and verify the detail immediately switches to the linked Restored entry with its action disabled; revisiting the source update or posting either entry again must not allow another restore. Make a direct arr edit before a separate restore and confirm the stale update is unavailable/refused. Validation detail must remain useful and redacted. For Radarr/Sonarr, also verify language IDs are fetched from each live catalog rather than reused across services or versions, and language scoring affects future release selection only—not file default tracks or playback language.
- [ ] `AI-020` · P1 · LIVE — Against a real local OpenAI-compatible server (llama.cpp `llama-server` with `--jinja`, vLLM, or Ollama's `/v1`), select the Local (OpenAI-compatible) provider in Settings > Providers & Credentials with the server's base URL and model ID and no API key; verify the save-time turn reaches the local server (its request log shows `/v1/chat/completions` with the placeholder bearer) and activation succeeds, a chat turn with tool calls completes against it as included access, one autonomous remediation turn resolves to it, and a user's personal OpenAI key saved at the same time still validates against api.openai.com rather than the local endpoint. Verify a non-admin's AI Access screen never offers the Local provider. Attempt to save the Local provider without a base URL or model and verify the save is refused with nothing stored. Pin the reasoning effort to None and verify the save-time turn and a chat turn skip thinking (the local server's log shows the effort; turns complete in seconds), then set Auto and verify the effort field disappears from requests.

## MCP with a real external client

- [ ] `MCP-006` · P0 · SEC/LIVE — Complete authorization-code flow with S256 PKCE, state, exact redirect/resource/scope, and password; verify code is one-time/short-lived and wrong verifier/redirect/resource fails.
- [ ] `MCP-007` · P0 · LIVE — Complete secure-browser passkey authorization and create a first passkey from MCP login for a connect-link-only user; verify correct Cantinarr identity/device.
- [ ] `MCP-008` · P1 · API/LIVE — On disposable current Radarr, Sonarr, and Chaptarr instances, use `upsert_custom_format` to create one native/TRaSH format and repeat by the same exact name with changed rules; verify one record exists, create added it to every profile at score 0, and update preserved profile scores and changed the format definition (stored file matches need not recompute). In Settings > Configuration history, verify both MCP writes are readable with their target, actor/source, outcome, and before/applied/current comparison. Change or remove the disposable format directly in the arr UI/API and verify history reports the drift; confirm no restore is offered for either custom-format entry.
