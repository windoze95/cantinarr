# Claude Instructions

@AGENTS.md

`AGENTS.md` is the canonical operating manual for this repo (git workflow, verification, conventions, documentation standards). This file exists only so Claude Code loads it — make every change in `AGENTS.md`, never here, so all agents read one playbook.
